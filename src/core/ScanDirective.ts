import Parser from 'tree-sitter';
export class ScanDirective{
    // Ignore a node

    static ignoreNode(node: Parser.SyntaxNode){
        node.children.forEach
    }

    // Justify an exception
    // Replace tokens in message (?)
}